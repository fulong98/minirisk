# General
.DS_Store
Thumbs.db
*.log
*.swp
*~
.vscode/
.idea/

# Secrets - IMPORTANT: Never commit sensitive files like .env
# Add any other secret files here
.env
backend/.env
frontend/.env

# Go / Backend
# Compiled binaries
*.exe
*.out
minirisk-backend # Assuming this might be the binary name, adjust if needed
backend/minirisk-backend # Or if built inside backend/

# Node.js / Frontend
frontend/node_modules/
frontend/build/
frontend/npm-debug.log*
frontend/yarn-error.log*
frontend/.pnp.*

# Docker (usually covered by general ignores, but can add specifics if needed)

# Database (if any local db files are generated)
# *.db
# *.sqlite
